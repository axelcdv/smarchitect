# Anchor Room information with labels

Room geometry remains derived from Walls, while each Room Label anchors its name and other information at a point inside an enclosed space. When a partition splits a Room, the resulting space containing the label retains the information; when a merge produces multiple labels in one space, the editor asks the Homeowner to resolve them.
