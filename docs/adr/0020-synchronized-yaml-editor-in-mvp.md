# Include a synchronized YAML editor in the MVP

The MVP will provide an in-app YAML editor synchronized with the graphical editor, with inline validation and error reporting before textual changes become active. Complete Project Document import and export remain available for external tools and long-term portability.
