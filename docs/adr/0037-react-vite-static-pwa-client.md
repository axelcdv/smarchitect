# Build the client as a React and Vite static PWA

The MVP web client will be a React single-page application built with Vite and packaged for offline use as a static progressive web app. React remains an interaction shell around runtime-neutral domain packages; the core model, geometry, validation, migrations, and operations cannot depend on React or a server runtime.
