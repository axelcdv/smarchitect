# Derive Wall junctions from geometry

Project Documents will store independently editable Wall paths rather than explicit junction objects. The topology engine derives junctions from path intersections and endpoint contacts, while graphical snapping helps authors create exact contacts without requiring manual or AI tools to maintain a separate connectivity graph.
