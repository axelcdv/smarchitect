# Ship a local-first accountless MVP

MVP projects will autosave in browser-local storage and support explicit import and export of the complete Project Document without requiring an account. Cloud persistence, cross-device synchronization, and collaboration are deferred, preserving the open document as a first-class artifact rather than a secondary export.
