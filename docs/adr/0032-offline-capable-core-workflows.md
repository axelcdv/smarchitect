# Keep core MVP workflows available offline

After the application has loaded once, drawing, YAML editing, validation, autosave, Undo and Redo, Checkpoints, and Project Document or Project Archive import and export will continue working without internet access. Future cloud and hosted AI integrations may require connectivity but cannot become dependencies of the core editor.
