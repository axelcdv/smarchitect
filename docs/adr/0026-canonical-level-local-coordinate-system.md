# Use a canonical Level-local coordinate system

Every Level uses a right-handed coordinate system where `x` increases rightward, `y` upward in plan view, and `z` upward from the finished floor. Rotations use degrees counter-clockwise from positive `x`, normalized to `[0, 360)`, and each Level may choose a convenient local origin.
