# Store measurements as integer millimetres

Coordinates and physical dimensions in the Project Document will use integer millimetres as their canonical representation to avoid floating-point drift and keep file edits predictable. Initial versions will expose metric units only; display and input in inches and feet may be added later without changing stored geometry.
