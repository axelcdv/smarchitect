# Use the Project Document as the source of truth

The graphical editor, manual edits, and AI tools will all operate on one versioned, schema-validated Project Document. Its human-facing serialization may be YAML, but the domain model will not depend on YAML, and no essential project geometry or content will exist only in a database or proprietary binary representation.
