# Validate Project Documents in two phases

Restricted YAML Project Documents will parse into a JSON-compatible data model whose public structure is defined by JSON Schema Draft 2020-12 with an explicit dialect identifier. A second shared validation phase will enforce cross-references, hosted Opening bounds, geometry invariants, and other semantic rules that JSON Schema cannot express.
