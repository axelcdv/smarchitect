# Persist transaction-level Undo and Redo

Every accepted GUI operation, applied YAML draft, or AI operation batch will form one undoable transaction. Undo and Redo history will persist with local autosave across browser sessions, while named Checkpoints remain the intentional milestones included in a Project Archive.
