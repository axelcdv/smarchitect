# Apply YAML drafts explicitly and atomically

Text changes remain a draft until the Homeowner explicitly applies a fully valid document. While that draft differs from the active Project Document, the graphical editor is read-only; when no text draft exists, graphical operations immediately update the YAML view. This prevents simultaneous graphical and textual sources of truth.
