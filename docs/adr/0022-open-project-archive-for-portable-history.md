# Package portable history in an open Project Archive

The current project state remains exportable as a plain YAML Project Document. Full-fidelity transfer uses a ZIP-based Project Archive containing `project.yaml` and separate YAML files for named Checkpoints, keeping history portable and inspectable without embedding it into the active document.
